[tool.poetry]
name = "dagster_university"
version = "0.1.0"
description = ""
authors = ["coffeeBeanz <tomas.pousette@gmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
